[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PyBitEngine"
version = "0.1.0-alpha"
description = "Motore grafico 2D GPU-accelerato per Python con rendering instanced, texture atlas e camera 2D."
readme = "README.md"
authors = [
    { name = "BitufStudio", email = "bitufmusic@gmail.com" }
]
license = { text = "MPL-2.0" }
requires-python = ">=3.8"

classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Mozilla Public License 2.0 (MPL 2.0)",
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

keywords = [
    "pybitengine", "game-engine", "2d-graphics", "gpu-rendering", "modern-gl",
    "sdl2", "numpy", "numba", "texture-atlas", "batch-rendering"
]

dependencies = [
    "PySDL2>=0.9.7",
    "moderngl>=5.7.0",
    "numpy>=1.21.0",
    "numba>=0.56.0",
    "Pillow>=9.0.0",
]

[project.urls]
Homepage = "https://github.com/BitufStudio/PyBitEngine"
Documentation = "https://BitufStudio.github.io/PyBitEngine/"
Repository = "https://github.com/BitufStudio/PyBitEngine.git"
Issues = "https://github.com/BitufStudio/PyBitEngine/issues"

[tool.setuptools.packages.find]
where = ["src"]